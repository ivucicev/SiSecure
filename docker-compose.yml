# Builds the image from this directory. For the prebuilt GHCR image instead,
# use docker-compose.ghcr.yml.
services:
  sisecure:
    build:
      context: .
      dockerfile: Dockerfile
    image: sisecure:local
    container_name: sisecure
    expose:
      - "80"
    restart: unless-stopped
